# /village:board

Show a read-only at-a-glance view of village state.

## Template

Show the current village board — a summary of all beads across roles and statuses.

Call `village_board` to render the board. If the user specified an epic ID, pass it as the `epic` argument to scope the view to that epic's children.
